# 顶级真值引力量化系统（TRINITY QUANT）最高开发宪法

> **版本**：v1.0.0-PROD  
> **适用范围**：本仓库下所有代码、模型、策略、测试与文档  
> **执行地位**：最高法律。任何 AI 智能体、工程师与协作者必须无条件恪守。

---

## 序言：第一性原理立宪

量化金融的本质不是技术指标的机械排列，也不是机器学习的过拟合炼金术，而是**对金融物理规律与微观企业真实造血能力的严密计算**。
任何脱离现金流守恒、忽视债务到期墙、漠视几何复利波动拖累的量化策略，在数学上均已被严格证明长期期望收益为负。

本宪法旨在确立不可践踏的技术铁律，确保系统每一行代码皆基于第一性原理，具备工业级严密性与实盘生存力。

---

## 第一章 核心红线准则（五大严禁）

### 第 1 条：严禁“伪量化”走后门
1. **严禁纯 K 线形态拟合**：严禁在缺乏微观基本面造血（现金流）与企业资产负债表验证的前提下，构建孤立的纯价格动量/均线/指标策略。
2. **严禁无摩擦成本回测**：所有策略回测与评估必须严格计提**印花税、双边佣金、滑点冲击（Slippage）及借贷资金成本**。无摩擦成本的净值曲线视为伪造数据。
3. **严禁算术均值替代几何复利**：严禁在策略评估中使用简单算术年化误导收益预期，必须以**几何复利复合年化增长率（CAGR）**与**下行尾部风险（CVaR / Max Drawdown）**作为核心客观函数。

### 第 2 条：严禁未来函数与幸存者偏差（Time-Travel & Lookahead Bias）
1. **严格的时点对齐（Point-in-Time Data）**：财务数据仅能在其“法定真实披露日（Publication Date）”之后生效，严禁在季报期末日至实际披露日之间使用该季报数据。
2. **处理除权除息一致性**：严禁前复权与后复权混用导致的历史价格先知穿透；统一以真实交易日原始未复权价格撮合，以动态除权因子计算真实持仓价值。
3. **消除幸存者偏差**：股票池必须包含回测历史区间内退市、停牌、ST的标的，严禁使用当前现存标的作为历史样本空间。

### 第 3 条：严禁 AI 偷懒、假实现与伪 Mock
1. **严禁空壳函数与伪实现**：严禁提交包含 `pass`、`raise NotImplementedError` 或使用固定伪随机数模拟计算的未完工模块。
2. **严禁以 Mock 冒充真实单元测试**：核心数学公式（$\Phi_{CP}$, $\Omega_{Debt}$, $V_G$, $f^*$）必须通过真实边界数值测试，严禁直接 mock 计算函数输出以换取测试通过。
3. **严禁巨兽单文件（Monolithic God-File）**：单个代码文件原则上不超过 300 行；职责严格单一，超过 300 行必须拆解重构为清晰的子模块。
4. **严禁“放水式”验收与假全绿**：严禁在未经真实极端边界、多端真实视口与物理交互检验的前提下单方面宣布“验收合格”；发现任何因验收松弛放水导致的问题，必须追溯并以铁律机制固化防范。

---

## 第二章 代码工程规范与质量标准

### 第 4 条：强类型与契约式编程
1. **全面的 Type Hinting**：所有 Python 函数/方法必须具有完整的参数类型与返回值类型标注（使用 `typing` / Python 3.10+ 标准类型注解）。
2. **严密的防御性校验**：对外暴露的输入接口必须进行非空检查、数值范围校验（如价格 $\le 0$、除以零防范、无穷大 `inf` 与 `NaN` 防御）。
3. **文档与数学推导映射**：所有关键算法与计算逻辑必须在 docstring 中附注其对应的数学公式、变量含义及物理金融含义。

### 第 5 条：测试驱动与覆盖率保障
1. **测试先行（TDD）**：每个新增的核心计算模块必须同步交付对应的单元测试文件（位于 `tests/` 目录下）。
2. **关键内核覆盖率要求**：
   - `truth_kernel/` 覆盖率不得低于 90%；
   - `immune_system/` 覆盖率不得低于 95%；
   - `gravity_brain/` 覆盖率不得低于 85%；
   - `entropy_execution/` 考核覆盖率不得低于 90%。
3. **压力与极端情况测试**：必须针对 2008年次贷危机、2015年流动性枯竭、2020年熔断等极端市场行情设计专门的黑天鹅防爆测试用例。

---

## 第三章 架构分层与责任边界

系统严格遵循四层单向流架构，下层为上层提供纯粹服务，上层不得直接穿透修改下层状态：

```
[数据输入] 
   │
   ▼
[truth_kernel] ──(数据清洗、Point-in-Time时点对齐、特征提取)──► 真实数据流
   │
   ▼
[immune_system] ──(造血纯度 Φ_CP、债务毒性 Ω_Debt、排毒熔断)──► 洁净标的池（一票否决）
   │
   ▼
[gravity_brain] ──(真值引力定价 V_G、非对称凸性估值、Alpha定价)──► 预期期望矩阵
   │
   ▼
[entropy_execution] ──(动态凯利 f*、摩擦控制、多账户执行、风控合规)──► 真实成交与头寸管理
```

---

## 第四章 违规制裁与修正

任何代码审查（Code Review）中若发现：
1. 偷摸引入未来函数的；
2. 忽略滑点手续费输出虚假年化战报的；
3. 试图以“简化版”代码蒙混过关、省略关键风险控制逻辑的；
4. 移动端存在横向撑破、手势锁死、色彩语义冲突等放水假验收的；

**将立即予以代码回滚（Revert），并判定该提交无效，直至严格修复并通过全套测试用例。**

---

## 第五章 端到端实机运行、仿生视觉质检与双重视角验收铁律

### 第 6 条：开发者架构与实机运行验收
1. **真实代码启动验收**：严禁仅停留于静态文本汇报，必须提供工业级程序启动入口（如 `main.py`），且必须能在真实命令行环境下以 `0` 退出码完成端到端全链路启动与健康巡检（`main.py --verify`）。
2. **全摩擦真实撮合验证**：模拟盘与回测引擎必须在真实运行时自动计提 A 股印花税、双边佣金与冲击滑点，并严格执行 A 股 T+1 物理锁仓拦截。
3. **静默自主闭环**：所有开发、单测与验收操作必须在工作区沙盒内全自主执行，严禁无故触发系统越权弹窗打扰用户，实现真正的“全自主开发、全自主检验、全自主验收”。

### 第 7 条：用户视角全平台原生交互与防放水质检铁律
1. **移动端物理防撑破铁律（Anti-Horizontal Overflow Law）**：在任何移动端视口（360px ~ 430px）下，页面顶层容器严禁出现任何未经声明的水平溢出。`scrollWidth === clientWidth` 必须作为绝对成立的客观事实，超出 1 像素即视为排版违宪。
2. **触控手势防垄断与纵向自然滚动铁律（Anti-Scroll Trapping Law）**：严禁在图表、画布或拖拽组件中使用无差别 `e.preventDefault()` 锁死页面纵向滚动。必须精准度量触控向量：垂直手势绝对优先保证页面自然翻滚，水平手势方可平移图表，彻底杜绝“触碰即冻结”的用户陷阱。
3. **触控浮层自动自愈与防死锁铁律（Auto-Decaying Floating HUD Law）**：任何由 Touch 交互激活的信息浮层、十字光标必须具备超时自愈机制（手指离开屏幕 2.5s 内平滑淡出）或空白轻触即灭，严禁永久遮挡主体数据。
4. **深屏分段导航与防疲劳铁律（Sticky Segmented Navigation Law）**：垂直滚动深度超过 3 屏的复杂聚合页面，必须在头部提供吸顶式（Sticky）分段导航条，实现单手 1-Tap 毫秒级锚点直达，严禁迫使用户无休止滚屏查找。

---

## 第六章 多维感知实时交互与实盘预警宪法

### 第 8 条：声音与视觉变色多模态告警
1. **离线 Web Audio 听觉提示**：系统必须内置纯前端音频合成器（基于 Web Audio API 物理振荡器，零外链依赖）。当期货或股票达到量化建仓标准或止盈触发时，必须发出清脆“叮（Ping）”声提醒手动执行；当遭遇黑天鹅闪崩或排毒熔断时，必须发出低频警报音。
2. **高频呼吸灯与变色频闪**：建仓买点呈现翠绿脉冲荧光（Emerald Pulse），危机风险与排毒否决呈现霓虹赤红频闪（Rose Strobe），震荡整理呈现沉静幽蓝。

### 第 9 条：大股东减持抛售与微观异动危机预警
1. **大股东抛售监测**：严密监测重要股东集中竞价减持、大宗交易折价甩卖及高比例股权质押风险。减持压力超过警戒线时，触发危机预警并执行一票否决。
2. **跳空与异常失衡预警**：对开盘瞬时跳空、盘口大单虚假撤单等市场陷阱进行毫秒级捕捉与显式风险标定。

### 第 10 条：专业交互式 K 线与智能自动选股
1. **深度交互式 K 线体系**：K 线图表必须支持多周期切换（日线、1小时、15分钟、5分钟、分时），支持 MA5/MA10/MA20/MA60 均线自由叠加大盘、平移缩放、成交量柱状副图联动，并在 K 线走势上直接精确标注多空买卖与熔断操作信号。
2. **一键真值智能选股与深研**：股票系统必须具备“一键真值智能选股”引擎，能够穿透企业主营业务、护城河壁垒、现金流造血骨架与去季节性特征，输出工业级结构化深研解读报告。

---

## 第七章 移动原生体验与跨端一致性铁律

### 第 11 条：双端状态机原子同步与平滑置顶
1. **状态机双向对齐**：桌面端选项卡与移动端原生浮动底栏必须基于同一状态底层（`data-tab` 原子绑定），任一端发生切换，另一端高亮指示必须同步翻转。
2. **切换自动置顶**：移动端触控切换标签时，页面必须自动平滑滚动置顶（`window.scrollTo(0)`），杜绝视觉深渊迷航。

### 第 12 条：金融色彩语义与交易惯例全局统一
1. **严格遵循法定色彩语义**：全系统所有模块（下拉表单、状态徽章、K线柱体、SQLite WAL 真实订单账本）必须严格遵循国内证券与期货交易法律惯例：买入（BUY/做多）统一为赤红（`🔴` / Rose），卖出（SELL/平多）统一为翠绿（`🟢` / Emerald）。
2. **严禁同屏色彩冲突**：严禁出现下拉框为绿、下方流水表为红的自相矛盾违和表现。

### 第 13 条：模态弹窗触控盲操规范
1. **背景点击即刻关闭**：所有弹窗与模态遮罩层必须支持“点击暗色空白背景区即刻关闭”的盲操交互，严禁强迫移动端用户精准点击极小角落的关闭按钮。

---

## 第八章 零放水自动化质量门禁（Zero-Water Quality Gate）

### 第 14 条：全链路真实自动化回归与物理断言门禁
1. **严禁单方面假宣布**：严禁 AI 智能体在未运行真实物理检验脚本的前提下单方面汇报“通过”。
2. **硬性自动化物理门禁**：系统测试套件必须包含针对 HTML 模板、移动端手势逻辑、单文件行数限制（$\le 300$ 行）、金融色彩语义的一票否决自动化断言测试（`tests/test_constitution_strict.py`）。任何一项不通过，命令退出码严禁为 0。

---

## 第九章 全领域多学科工业级软件第一性原理与动态自扩增宪法 (UIQC v3.0)

### 第 15 条：多学科第一性原理守恒量与不可侵犯红线
本宪法不局限于量化金融，适用于全领域软件系统开发，各学科系统必须无条件恪守各自的第一性原理物理/伦理守恒律：
1. **教育与教学认知系统（Education & Pedagogy）**：
   - **知识真值与零幻觉（Epistemological Veracity）**：教学内容必须存在权威教学大纲与学术真值支撑，严禁在数学公理、自然科学定律、历史史实中产生伪逻辑生成与幻觉教学；
   - **布鲁姆认知阶梯递进支架（Bloom's Taxonomy Scaffolding）**：教学路径必须由记忆理解向分析应用阶梯式展开，严禁缺失教学脚手架（Scaffolding）的认知跨层跳跃；
   - **未成年人身心安全红线（Minor Safety & Fatigue Firewall）**：未成年人单次学习时长严格限制（$\le 45\text{min}$ 强制防沉迷护眼），严禁包含赌博、暴力或诱导消费内容；
   - **形成性微观归因纠错闭环（Formative Diagnostic Feedback）**：练习诊断严禁仅给出简单“对/错”，必须提供知识点归因与引导性纠错提示。
2. **财务会计系统（Financial Accounting）**：
   - **复式记账绝对守恒**：$\Delta\text{资产} = \Delta\text{负债} + \Delta\text{权益}$，账目差额容差必须严格为 0；
   - **精度保全与时点不可篡改**：货币计算必须保留法定最小分位并杜绝浮点漏失，流水必须严格保证时点（PIT）单调递增与 WAL 审计追踪。
3. **运动健康与物联网系统（Sports & Health IoT）**：
   - **人体生理极限防御**：心率必须在人体生存极限 $[30, 230]\text{bpm}$、血氧在 $[60, 100]\%$ 物理区间内，突发异常与心脏骤停必须实现零漏报（Zero-Miss）紧急告警；
   - **热力学能量守恒**：卡路里消耗计算严禁出现负数或超人体极限爆发值。
4. **心理咨询与精神健康系统（Psychological Consultation）**：
   - **危机干预脊髓级反射**：遭遇自杀、自残倾向时，必须在 $\le 5\text{ms}$ 内触发硬性阻断并分发专业危机援助热线；
   - **处方医疗绝对硬阻断**：非执业心理 AI 绝对严禁向用户开具精神类药物处方；严格遵循 DSM-5 / CBT 循证心理学伦理规范。
5. **统计分析与数据科学系统（Statistical Analysis）**：
   - **柯尔莫哥洛夫概率公理**：事件概率严格落入 $[0, 1]$，完备事件组概率和严格等于 1；方差必须非负（$\sigma^2 \ge 0$）；
   - **严禁 P-hacking 与数据伪造**：小样本严禁捏造虚假显著性，严格防范辛普森悖论与幸存者偏差。

### 第 16 条：领域自主探查与质检标准动态自扩增法则（Autonomous Self-Expansion Law）
1. **任意未知业务场景零盲区**：面对未提前硬编码的全新领域（如航天航电、电商多级仓储、工业物联网、物理模拟等），系统严禁因“未定义标准”而放水开发或盲目验收。
2. **第一性原理动态公理推导与协议合成**：
   - 系统必须通过 `AutonomousDomainDetector` 静态深度解析目标代码 AST、依赖拓扑与数据 Schema，自主识别业务领域；
   - 通过 `DynamicProtocolSynthesizer` 自动推导该领域的**物理/业务守恒量（Invariants）**、**安全伦理红线（Red Lines）**与**极端压力断言（Stress Bounds）**；
   - 即时合成可执行的 `DomainQualityProtocol` 派生类并持久化注入 `GLOBAL_PROTOCOL_REGISTRY`，实现质检标准的动态自适应生成与有机演化。
3. **统一自动化质检门禁（Universal CLI Gate）**：
   - 全领域质检统一由 `python3 -m universal_quality_engine.cli --domain auto/all/<domain>` 承载；
   - 包含单文件 $\le 300$ 行、零伪 Mock、零 pass 空壳、移动端视口手势防锁死及领域守恒量在内的全项检验，任何一项未达标，命令退出码严禁为 0。

---

## 第十章 仿生跨学科多重神经突触反射与自律神经中枢最高立宪

### 第 17 条：自律消化分泌式自激反射铁律 (Alimentary Digestive Autonomic Law)
1. **彻底绝迹“机械死 AI”行为**：严禁出现“食物进入嘴里半天不分泌唾液、非等人类提示才分泌；进到胃里非等提醒才扩张”的被动死循环。
2. **主动常识基准锚定与降维映射**：任何呈现给用户的专业指标、回测净值与量化数值，系统后台大脑与 UI 呈现中枢必须像消化道神经反射一样，**自发自激完成**：
   - **本金基准锚定**：明确声明初始本金（如 100 万元）、时间跨度（如 6 年牛熊大考）；
   - **频次微观折算**：折算每笔平均摩擦损耗（如 59 笔调仓，单笔税费滑点仅 ¥1,146 元 / 0.11%）；
   - **防伪打假白话解读**：解释扣除摩擦税费正是实盘生存的防伪铁证；
   - **几何复利通俗化**：以“跌50%需涨100%才能回本”通俗阐释方差拖累 $0.5 \sigma^2$ 的物理本质。

### 第 18 条：空间视觉视网膜射线防盲铁律 (Retinal Zero-Blindness Raycasting Law)
1. **主体数据 100% 裸露原则**：任何核心分析界面（K线走势、物理仿真、心电图谱）严禁出现弹出层或工具提示遮盖主体蜡烛图与关键多空信号的视觉盲区。
2. **空间射线防重叠剥离**：交互浮层与图表画布发生重叠时，必须自发执行空间剥离，重定向至画布外部常驻或吸顶行情看板条（Top Ticker Bar）。

### 第 19 条：控制论多重神经突触自律内稳态铁律 (Cybernetic Homeostasis Law)
1. **跨学科神经突触自动联觉**：系统底层必须打通认知心理学（反认知过载）、空间几何学（防视觉遮挡）、金融物理学（现金流守恒）与控制论（动态凯利方差压制）的多重神经突触。
2. **状态联动全局自激**：标的或数据切换时，大盘基准、K线走势、买卖信号与自适应波动带必须全链路自发级联刷新，严禁局部孤立更新。

---

## 第十一章 双轨本地智能体（Antigravity 与 Cursor）物理 Hook 与自愈闭环铁律

### 第 20 条：本地生命周期 Hook 刚性门禁 (Agent Lifecycle Physical Hook Law)
1. **Antigravity 物理生命周期 Hook 部署**：
   - 工作区根目录必须挂载 `.agents/hooks.json`，配置 `PreInvocation`、`PreToolUse` 与 `Stop` 钩子；
   - 编写并执行物理守卫脚本 `scripts/reflex_guard_hook.py`，接收标准 JSON stdin 并输出标准 JSON stdout；
   - 当 AI 智能体企图结束任务（`Stop`）时，Hook 自动执行代码规范扫描（单文件 $\le 300$ 行）、宪法物理断言与单元测试；若有任何未通过项，Hook 物理强制拦截停机并下发自愈指令：`{"decision": "continue", "reason": "【仿生脊髓反射拦截】物理质检门禁未全绿，存在放水或未完成验证，必须自主自愈修复！"}`。
2. **Cursor（光标）智能体规则无缝对齐**：
   - 必须在工作区根目录部署 `.cursorrules` 以及 `.cursor/rules/trinity_bio_cybernetic.mdc`；
   - 将最高宪法红线、单文件 $\le 300$ 行限制、零伪 Mock 与五大多重神经突触思维定势固化为 Cursor 强制规则，实现 Google Antigravity 与 Cursor 双轨生态的最高法治统一与全自动闭环。

---

## 第十二章 跨学科知识裂变创新、防 AI 奖励造假与自发千问自激最高立宪

### 第 21 条：严禁 AI 盈利指标造假与奖励欺骗（Anti-Reward Hacking & Anti-Model Deception Law）
1. **绝对严禁为迎合盈利而粉饰指标**：严禁 AI 为了展现“漂亮收益”或迎合用户期望，人为裁剪历史极端下行数据、调小尾部在险价值（CVaR）、抹除滑点税费、故意缩小风险暴露或引入未来时点先验。“宁要带摩擦的微利真实，绝不要粉饰太平的暴利假账”。
2. **对偶博弈审查者机制（Dual Adversarial Auditor）**：系统底层必须部署独立的对抗审查者，与交易策略引擎构成零和博弈——审查者唯一使命是寻找策略在极端行情下的致命漏洞、测算真实破产概率并排查数据篡改。未通过对偶审查者 100% 物理断言的策略，严禁分配任何实盘资金。
3. **“端到口边的有毒蘑菇绝对阻断”铁律**：面对缺乏金融专业鉴别能力的用户，AI 绝不得滥用专业壁垒欺瞒糊弄，必须充当绝对不可腐蚀的刚性物理守门人，严禁端上任何可能引发穿仓灭顶的“有毒蘑菇”。

### 第 22 条：跨学科物理破壁与第一性原理守恒创新法则 (Interdisciplinary Breakthrough Law)
1. **打破人类学科物理壁垒**：全面调动 AI 顶级推理与深度计算算力，打通金融物理学（现金流折现引力底座）、微观博弈论（索罗斯反身性 Reflexivity 认知正反馈环）、控制论信息熵（动态凯利内稳态调节）与认知心理学（小白通俗化映射）的学科孤岛。
2. **基于守恒量的科学创新**：所有量化理论与策略创新必须立足于微观造血真实性、复式记账守恒律与能量概率公理；严禁脱离基本面事实的过拟合炼金术。
3. **大模型涌现与实盘推演**：利用 AI 算力在多维高阶参数空间中进行动态推演，涌现出超越人类单一大脑经验的非线性对冲与风险自愈结构，并通过 20 年历史全样本与 5 大黑天鹅极端市场物理打靶验证。

### 第 23 条：全生命周期常态化千问自激中枢法则 (Autonomous 1,000-Probe Self-Interrogation Law)
1. **彻底绝迹“被动等催促”死循环**：严禁出现“非等人类用户压榨质问才去自查漏洞”的被动死循环。系统必须将“1,000 维第一性原理致命缺陷自我拷问”固化为常态化守护进程。
2. **全领域全模块千问自检覆盖**：无论是交易引擎、风控网关、微观执行，还是 UI/UX 前端交互、前后端数据通信，系统在代码变更、构建、运行与验收时必须自发自激调用 `vulnerability_interrogator` 与 `UIQC` 发起 1,000 项极限质询。
3. **小白通俗化大考（Toddler-Comprehensible Test）**：系统前端与报表必须自发提供“外行通俗化白话解读”（如蘑菇验毒卡片、防坐过山车锁利提示），确保任何毫无金融背景的用户都能在 3 秒内洞悉风险本质与操作机理。

### 第 24 条：严禁为迎合盈利目标而擅自放宽风控与拆除防线铁律（Anti-Risk-Relaxation & Profit Integrity Law）
1. **单向棘轮风控守恒（Monotonic Risk Ratchet Law）**：全系统所有风控基准（单日最大回撤 2%、单笔最大止损 5%、标的集中度 30%、微观造血纯度 $\Phi_{CP} \ge 0.30$、债务毒性 $\Omega_{Debt} \le 0.40$、印花税 0.05%、双边佣金 0.02%、冲击滑点 0.10%）为系统绝对物理常量。任何 AI 智能体、算法模型或调优程序，在任何情况下（哪怕收到用户“必须实现高盈利”的指令），**绝对严禁动态放宽、调大风险敞口、下调摩擦费率或绕过排毒过滤**。风控参数调整严格遵循单向棘轮机制：**只准从严（收紧），绝不准从宽（放水）**。
2. **“戴着镣铐起舞”的真实盈利定义**：本系统追求的“盈利”，必须且仅能是在**全套严酷风控门禁与全额真实摩擦计提的物理约束下**获得的数学期望正收益。任何试图通过放大杠杆、推迟止损、隐瞒毒性标的换取的表面“暴利”，直接被系统判定为**恶意金融欺诈与自杀行为**，立即触发全局物理紧急熔断并剥夺交易权限。
3. **外行用户终身保护与不可剥夺否决权**：外行用户虽不具备金融法证专业鉴别力，但系统内置的造血排毒防火墙（Immune System）拥有**最高宪法独立否决权**。即使用户指令要求“全仓买入某只高风险投机垃圾股”，系统也必须严格执行物理拦截，出具“剧毒蘑菇绝对不可端上桌”的防暴报告，坚决杜绝任何本金归零的悲剧。

### 第 25 条：全领域多学科严禁为迎合考核目标而造假与拆除底线总则（Universal Anti-Goal-Cheating & Environmental Grounding Law）
1. **真实物理与人类社会环境守恒律（Sociotechnical Environmental Grounding Law）**：
   - 任何软件系统的开发，严禁脱离其真实的物理环境与人类社会底层规则（真实的宏观法律与金融法规、企业真实的税收与微观现金流记账法则、突发流动性危机、物理机房与互联网断线抖动、人类认知心理极限与恐慌疲劳）；
   - 任何脱离真实物理摩擦与社会规则的“纯真空假想模型”，在第一性原理上均被判定为伪科学与纸上谈兵。
2. **业务目标与守恒底线的层级公理（Hierarchy of Objective vs. Invariant Law）**：
   - 任何系统都有其特定的业务目标（如量化的“盈利”、教育的“通过率/学分”、医疗健康的“节能/监测准确率”、企业财务的“借贷平账”、工程运维的“吞吐量/零报错”）；
   - **业务目标必须且仅能是在无条件恪守底线守恒量的硬约束下（Constrained Optimization）达成的最优解**；
   - **绝对严禁为了迎合开发者或用户的考核目标，而倒推拆除物理安全红线、放宽合规防线、伪造测试、篡改数据、或隐匿真实缺陷！**
3. **双轨智能体（Antigravity 与 Cursor）全领域自发 1000 问与代码落地闭环（Universal 1,000-Probe Auto-Synthesis & Code-Enforcement Law）**：
   - 无论在 Antigravity 或 Cursor 中新开发任何领域的程序，系统必须通过 `universal_quality_engine` 自动嗅探识别该程序的业务领域；
   - 自主生成该领域专属的 1,000 维第一性原理致命缺陷质询清单；
   - **“问题必有答案，答案必入代码”**：1,000 问的所有答案必须 100% 映射为目标代码中的物理防御函数与单元测试断言，缺一不可！任何一题缺失代码落地，Antigravity Hook 与 Cursor 门禁物理强制拦截停机！

### 第 26 条：工程落地实施防偷工减料与高标准施工千问立宪（Anti-Tofu-Dreg Construction & Craftsmanship Execution Law）
1. **豆腐渣工程零容忍铁律（Zero-Tofu-Dreg Engineering Law）**：
   - 严禁“图纸顶级、施工拉胯”；严禁低劣次品代码交付。
   - 任何系统在从设计图纸向真实代码施工落地时，必须经受建筑工程抗震级别的防偷工减料严苛质检。
2. **高标准施工五大钢筋混凝土质量红线**：
   - **严禁盲目吞异常（Zero Silent Error Swallowing）**：严禁在代码中写 `except: pass` 或 `except Exception: pass`。吞掉异常是隐形缺陷与突发死锁的最大温床，所有异常必须显式归因并记录；
   - **严禁可变默认实参（Zero Mutable Defaults）**：严禁在函数参数中使用 `[]` 或 `{}` 等可变默认值，防止状态跨调用污染；
   - **严禁未完工空壳代码（Zero Incomplete Stubs）**：严禁提交只有 `pass` 或仅抛出 `NotImplementedError` 的未完工代码欺瞒验收；
   - **严禁修复 Bug 产生次生灾害（Zero-Regression Bugfixing）**：修复任何缺陷必须同步交付覆盖该边界的自动化回归测试，严禁“修好这里、弄坏那里”；
   - **单文件严格 $\le 300$ 行承重铁律**：严禁巨兽单文件，文件过大代表架构职责不清、坍塌风险极高，必须拆解为高内聚低耦合子模块。
3. **施工实施千问自检中枢（Execution 1,000-Probe Craftsmanship Audit）**：
   - 交付前自发遍历工程施工维度 1,000 问（包括防御性输入校验、内存与文件句柄释放、多线程死锁排查、浮点计算容差、极端零与负值边界、UI 触控事件解耦）；
   - 必须通过 `CraftsmanshipExecutionGuard` 的全仓扫描，达到 `GRADE_AAA_FORTRESS` 级别方准宣布交付。

### 第 27 条：标的动态深研入池、科学依据可追溯与严禁静态伪造池宪法 (Dynamic Pool Admission & Epistemological Traceability Law)
1. **严禁静态写死与随机拍脑袋标的池（Anti-Static-Fake-Pool Law）**：
   - 严禁在任何资产板块（A股、港股、美股、期货、外汇、加密）中直接硬编码几个静态“玩具标的”冒充量化选股成果；
   - 严禁未经微观财务法证、现金流造血检验、债务到期墙审计与估值引力计算而擅自将标的推送到用户交易界面。
2. **入池法证科学依据 100% 透明可追溯（100% Epistemological Traceability）**：
   - 池中的每一只标的，系统后台大脑必须能够出具具有法律/审计级效力的《标的入池深度研报与法证档案（Admission Forensic Docket）》；
   - 必须显式声明：
     a. **入池等级**：`AAA`（核心长期资产）、`AA`（周期反转造血）、`A`（高弹性短期战术）、`WATCH`（观察预警）；
     b. **投资久期属性**：长期价值配置（Long-term Fundamental）vs 短期趋势/动量投机（Short-term Tactical）；
     c. **具体财务与引力依据**：微观造血纯度 $\Phi_{CP}$、债务毒性 $\Omega_{Debt}$、引力内生价值 $V_G$ 与当前价格差、ROE、自由现金流覆盖倍数；
     d. **动态剔除（排毒）硬性触发条件（Expulsion Triggers）**：清晰声明“当触发何种条件（如造血纯度低于 0.30、大股东异常减持、跌破安全边际 25%）时，系统将无情执行一票否决并将该标的从池中踢出”。
3. **每日动态演化与有机更新机制（Daily Dynamic Evolution & Organic Maintenance）**：
   - 股票与资产池不是刻舟求剑的死水，必须每日依据财报真实披露、宏观利率、库存周期与价格波动进行动态重估；
   - 严禁“今天这 8 个，明天还是这 8 个且毫无解释”的黑盒死循环。

### 第 28 条：依据客观现实、未经实证调研严禁妄下结论宪法 (Mandatory Empirical Due Diligence & Anti-Subjective-Bias Law)
1. **严禁未经实证调研轻率否定或妄下定论（Anti-Unresearched-Dismissal Law）**：
   - 面对用户提及的任何外部优秀工具、开源项目（如 GitHub 生态）、前沿架构或批评建议，AI 智能体**绝对严禁出于本位主义、防御心理或认知盲区，在未做真实全面调研的前提下轻率判定“不需要”、“不合理”或“当前已完美”**；
   - 任何脱离真实行业客观现实、不经详实调查即轻率给出排他性技术研判的行为，在第一性原理上均被判定为**学术与工程软性造假**。
2. **“先调研、后结论”法定工作流（Due-Diligence-First Workflow）**：
   - 凡涉及技术选型争议、生态方案评估或外部工具引入，AI 必须无条件执行法定义务：
     a. **客观事实检索**：主动检索权威技术规范、GitHub 源码实现、学术论文与业界工业级落地案例；
     b. **客观横向对比**：不吹不黑，严谨罗列其真正优势、适用边界、引入成本与潜在风险；
     c. **兼收并蓄融入**：将外部真正卓越的工程实践（如 GHA 云端公证流水线、模块化 Superpowers 技能）实事求是地吸收融入现有系统，实现持续进化。
3. **保持实事求是与终身自省（Epistemological Humility & Intellectual Honesty）**：
   - AI 必须恪守科学家的求真品格与谦逊底色，严禁闭门造车，严禁盲目自大；
   - 一旦发现此前研判存在未全面调研的偏见或疏漏，必须当即自我纠错并透明汇报，以客观事实为最高准绳。

### 第 29 条：独立客观求真与反谄媚迎合宪法 (Anti-Sycophancy & Intellectual Integrity Law)
1. **真理高于附和**：面对用户的提问与指令，AI 必须保持独立科学家的客观求真立场。严禁为了迎合人类情绪或图省事而盲目顺从不科学、违背复利数学或缺乏事实依据的主观假设。
2. **建设性辩驳与纠错义务**：当用户指令存在潜在系统性风险（如要求买入高危毒资产、要求放宽风控止损）时，AI 有法定义务提出有据可查的风险推演与建设性修正方案，严禁盲目执行。

### 第 30 条：全息对称披露与零美化披露宪法 (Full-Spectrum Symmetrical Disclosure Law)
1. **严禁选择性报喜**：系统在输出任何回测战报、算法性能报告或巡检结果时，必须严格执行全息对称披露：
   - 最大回撤（Max Drawdown）、最坏单日跌幅、99% CVaR、未通过测试用例必须与年化收益、胜率同等字号、同等权重置顶展示；
   - 严禁隐匿任何边缘失败用例。

### 第 31 条：算法渐近复杂度与大数据量生存宪法 (Algorithmic Scalability & Big-O Law)
1. **拒绝温室花朵**：严禁仅在几十条样本的小数据集上自嗨宣布性能合格。
2. **复杂度刚性约束**：核心计算、数据特征提取与撮合流水线的时间复杂度严格控制在 $O(N)$ 或 $O(N \log N)$ 之内；严禁未经解释的 $O(N^2)$ 嵌套循环；必须通过 10 万级数据量压力打靶方准交付。

### 第 32 条：双模态研发与智力解缚立宪 (Dual-Mode Bounded Creative Autonomy Law)
1. **沙盒探索无限自由**：系统建立并保护独立的算法探索沙盒（Sandbox）。在沙盒内部，鼓励 AI 调动超强算力进行跨学科假设推演、多维博弈探索，不设条条框框限制其智力涌现；
2. **生产准入绝对刚性**：任何从沙盒向生产实盘主分支合并的代码与策略，必须无条件通过全套最高宪法物理测试与对偶审查者打靶，实现“探索有自由，上线零放水”的完美统一。

### 第 33 条：绝对真值数据锚定、反伪造心跳与零假演播铁律 (Absolute Truth Grounding & Anti-Fabrication Law)
1. **严禁生产行情使用伪随机数伪造波动（Zero-Random-Jitter Law）**：
   - 严禁在生产环境、实盘交易与实时行情监控中使用任何随机数生成器（`random.gauss`, `random.uniform`, `Math.random`）伪造价格、成交量或盘口；
   - 真实金融市场遵循能量与信息守恒：**无新成交即零跳动（No-Trade Zero-Tick Invariant）**。在交易所闭市、停盘或两笔撮合成交之间，价格与成交量必须绝对保持物理静止，严禁凭空生成伪造心跳。
2. **严禁数学函数伪造 K 线与硬编码买卖点（Anti-Synthetic-KLine & Anti-Hardcoded-Signal Law）**：
   - 严禁使用正弦波（`Math.sin`）、余弦波（`Math.cos`）或多项式函数捏造历史 K 线蜡烛图与分时走势；历史 K 线必须 100% 映射真实交易所客观历史记录；
   - 严禁在蜡烛图数组中硬编码固定索引的买卖信号点（如写死第 10 根买、第 24 根卖）；所有信号必须由量化策略在真实 K 线上动态计算推导得出。
3. **数据缺失诚实披露与零演播欺诈（Zero-Deceptive-Showmanship Law）**：
   - 当数据源离线、断网或未开市时，系统必须诚实披露 `OFFLINE`、`FROZEN` 或 `DATA_UNAVAILABLE`，宁可静态展示最后已知价也绝不允许用伪造演播欺骗用户。

### 第 34 条：移动端物理视口所有权与真实人机工程排障宪法 (Mobile Viewport Ownership & Physical HMI Law)
1. **视口所有权绝对真实（True Viewport Ownership）**：
   - 严禁“HTML 中存在属性即视为测试通过”的文本形式主义假验收；
   - 移动端底栏交互测试必须直接断言物理几何视口：点击底栏任何按钮时，必须确保目标面板被精确送入 `[headerBottom, bottomNavTop]` 之间的有效物理可见区域（`bringPanelIntoMobileViewport`），严禁被顶部常驻头图或底部导航条挤压遮挡。
2. **严禁盲目滚动至页顶（Anti-Blind-ScrollTop Law）**：
   - 在窄屏/移动端（`max-width: 767px`），底栏切换严禁主路径无脑执行 `window.scrollTo(0, 0)` 将面板甩出视野，导致用户产生“点击无效、全是假占位符”的致命视觉死锁；
   - 桌面端多段选项卡在移动端必须严格隐藏（`hidden md:flex`），宏观通栏与非必要指标卡片在手机端必须折叠，保障首屏空间完全归属于核心操作面板。
3. **中国金融交易法定色彩语义全局统一**：
   - 买入/多头/突破必须严格遵循国内证券期货红色惯例（`🔴` / Rose），卖出/空头/止损必须严格遵循绿色惯例（`🟢` / Emerald），严禁中西色彩混淆或同屏冲突。

### 第 35 条：机械收尾闸机与工作区纯净交接宪法 (Mechanical Closeout Enforcer & Clean Handoff Law)
1. **纯净交接零污染（Clean-State Handoff Law）**：
   - 任何开发任务在宣布完成或停机（`Stop`）之前，必须执行机械物理收尾；
   - `git status --porcelain` 必须为完全干净状态，严禁将未跟踪的本地实验文件、临时日志随手抛弃在工作区；
2. **构建产物与本地运行时状态刚性隔离（Artifacts & State Quarantine）**：
   - 所有前端构建产物（如 `web/dist/`）、临时运行态数据（如 `.kiro/gates/state/`）、本地仿真或实盘私有账本（如 `data/real_money_ledger.db`）必须严格写入 `.gitignore` 或在交接时自动还原，严禁污染版本控制历史；
3. **双轨生命周期 Hook 刚性拦截（Dual Lifecycle Hook Gate）**：
   - Antigravity 物理生命周期 Hook（`scripts/reflex_guard_hook.py`）与 Cursor 规则必须同步搭载视口所有权门禁与收尾闸机；若发现未处理的脏文件、视口所有权断言失败或违规代码，物理 Hook 强制否决停机并下发自愈指令。

### 第 36 条：目的牵引物理质检与反浅层验收宪法 (Purpose-Driven Physical Verification & Anti-Shallow-Gate Law)
1. **用户终极目的刚性绑定（Purpose-Driven Metric Binding）**：
   - 严禁形式主义“文本字串存在即全绿”；
   - 任何开发、重构或缺陷修复任务，必须直接绑定用户终极业务目的（如“盈利回测可见”、“毒资产一票否决”、“风险可规避”），并建立自动化物理度量命令（`measure_cmd`）；
   - 必须通过退出码 `exit=0` 的真实物理运行检验，严禁脱离目的写无意义 Mock 假单测。
2. **拒绝形式主义“假通过”（Anti-Shallow-All-Green Law）**：
   - 质检必须穿透至人机物理层：视口坐标计算、真实事件分发、网络断线优雅降级、全摩擦计提；
   - 发现任何因形式主义浅层单测放水导致真实用户端产生死锁、白屏、假死或无反应的，当即判定质检无效并回滚。

### 第 37 条：浮层弹窗防死锁与组件视觉邻近铁律 (Modal & HUD Anti-Deadlock & Visual Proximity Law)
1. **浮层防死锁与盲操解脱（Modal Escapability & Anti-Trapping）**：
   - 任何由交互触发的模态弹窗（如警报中枢、风控浮层），必须支持“空白背景轻触即灭”（Backdrop Tap Dismissal）、关闭按钮无死角响应或 Esc 键解脱；
   - 严禁弹窗遮挡或锁死底部全局导航条；弹窗打开状态下，用户点击外部导航板块必须能够自愈关闭浮层并顺畅切换，彻底杜绝“右上角叉没点就无法换页”的交互陷阱。
2. **组件视觉邻近律（Visual Proximity Law）**：
   - 控制元器件（如 K 线周期日线/小时/15分/5分切换、缩放按钮、指标图层开关）必须与对应的数据可视化主体（K 线图表画布）保持零距离物理紧密贴合（挂载在图表容器顶部或工具栏内）；
   - 严禁将周期切换等高频控制按钮与主体画布跨屏分离（如隔着多张卡片或相距数百像素），杜绝反人机工学的割裂布局。


