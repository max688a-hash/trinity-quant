---
description: 军工级代码防豆腐渣与极速解耦重构超级技能 (Craftsmanship Guard Superpower)。全仓扫描单文件<=300行、零吞异常、零可变默认实参、零未完工空壳。
globs: *
alwaysApply: true
---

# Cursor Superpower: 军工级防豆腐渣与极速解耦重构 (Craftsmanship Guard)

## 触发场景
代码编写、重构、修 Bug、交付审查时全局常驻生效。

## 执行准则
1. 承重铁律：任何 Python 文件严格 $\le 300$ 行，超出必须主动拆解重构；
2. 零盲吞异常：严禁 `except: pass`，所有异常必须显式归因；
3. 零可变默认实参：严禁 `def f(x=[])` 或 `def f(d={})`；
4. 零未完工空壳：严禁 `pass` 占位，必须交付真实实现与防御逻辑；
5. 绝对真值数据锚定（Article 33）：严禁使用伪随机数（random.gauss, Math.random）伪造行情与波动，闭市价格恒定；严禁正弦波造假 K 线与硬编码买卖点；
6. 机械收尾闸机（Article 35）：交接或停机前必须执行机械收尾，`git status --porcelain` 必须为完全干净状态；构建产物（web/dist/）、运行态与私有账本隔离写入 .gitignore；
7. 目的牵引物理质检（Article 36）：严禁文本形式主义字串假绿；必须绑定用户目的指标并执行真实物理测度命令（measure_cmd），退出码 exit=0；
8. 全仓必须达到 `GRADE_AAA_FORTRESS` 军工堡垒级标准。
9. 禁止埋地雷（第 38 条）：已知缺陷必须当场修复或标 DATA_UNAVAILABLE；严禁诚实遗留；收尾闸不得放水。
